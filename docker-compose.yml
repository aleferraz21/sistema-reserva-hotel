version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "5173:5173" # Mudamos para a porta padrão do Vite
    depends_on:
      - backend